import React from 'react';

function Header({ categories, onCategoryChange }) {
    return (
        <header>
            <h1>Product List</h1>
        
        </header>
    );
}

export default Header;
